# 更新日志 (CHANGELOG)

## v1.0.91 (最新版本)

### 🧠 新功能：Prompt 优化器全面升级

- **数据上传 (TrackingEvent)**
  - 新增「数据」Tab，支持在画布内直接上传评测数据（变量值、模型回答、参考答案、人工评分）。
  - 这是启动 Prompt 优化的前提条件，无需再手动调用 API。

- **优化对比报告 (GetReport)**
  - 优化完成后自动拉取对比报告，展示原版 vs 优化版的评分对比和 Prompt 文本差异。
  - 支持一键复制优化后的 Prompt。

- **AI 生成评估标准 (CriteriaGeneration)**
  - 评估 Tab 新增「🤖 AI 生成」按钮，可根据示例数据自动生成评估标准文本。

- **版本管理 (Get/List/Update PromptTemplate)**
  - 优化 Tab 底部新增版本管理区域，支持列出所有版本、查看版本详情、编辑保存、使用历史版本。

### 💾 新功能：项目保存与导入导出

- **快速保存组件**
  - 画布右下角新增 💾 按钮，点击弹出操作菜单。
  - 快速保存后弹窗显示保存详情（项目名、节点数、连线数、历史条数、时间）。

- **项目导出 (JSON v3.0)**
  - 导出完整项目数据，包含节点信息、文本内容、位置坐标、连线关系、视图状态、生成历史。
  - JSON 文件内含元数据（节点统计、类型分布），便于快速了解项目概况。

- **项目导入**
  - 导入 JSON 后自动恢复画布状态。
  - 智能识别：本机导出的画布可完整恢复生成历史和预览图；他人的画布仅恢复节点和连线，并提示用户。

- **兼容旧版 JSON**
  - 保留旧版导入导出入口（v2.8 格式），确保向下兼容。

### ✨ 体验优化

- Prompt 优化器节点尺寸增大（580×780 → 640×900），容纳更多功能区域。
- Tab 布局从 3 个优化为 4 个（生成 / 数据 / 优化 / 评估），功能分区更清晰。
- 保存成功弹窗 5 秒后自动消失，不干扰操作。

### 🔧 修复

- 修复 SSE 流式解析中 `keep-alive` 心跳包导致生成结果为 "null" 的问题。
- 优化失败时显示具体错误信息，并提示可能原因（如缺少评测数据集）。

## v1.0.9

### 🎬 新功能：参考视频支持（Seedance / Doubao 模型）

- **参考视频拖拽上传**
  - AI 视频节点新增参考视频区域，支持拖拽导入或点击按钮选择本地视频文件（mp4/mov/avi/webm/mkv）。
  - 导入后自动截取首帧生成缩略图预览，支持双击缩略图打开灯箱全屏查看。
- **阿里云 OSS 直传**
  - 本地视频文件通过 Electron 主进程直连阿里云 OSS 上传，安全高效，无需额外后端服务。
  - 上传完成自动获取 CDN 公网 URL（image.lingjingxinghe.cn），直接传递给 Seedance API。
  - 上传失败时自动回退至火山引擎素材库，确保生成不中断。

- **参考视频 URL 粘贴**
  - 点击蓝色链接按钮可展开 URL 输入框，支持手动粘贴视频链接作为参考源。

### ✨ 体验优化

- **提示词 @ 引用支持视频**
  - 在提示词输入框中输入 `@` 现在可以引用参考视频（🎬 视频1、视频2…），与图片/音频引用一致。
- **视频缩略图灯箱预览**
  - 双击参考视频缩略图可打开灯箱全屏播放，操作体验与参考图一致。

## v1.0.8

### 🎨 界面升级

- 暗色模式粒子背景优化
- 画布交互体验提升
- 资产库界面亮度调整

## v1.0.5

### 🌟 核心功能提升 (Core Updates)

- **多项目状态完全隔离**
  - 【生成历史】与【资产库】现在完全绑定至独立项目。新建或切换不同项目时，各面板数据将保持纯净、互不干扰。解决了曾经出现的串台覆盖与内容混合的问题。
- **更人性化的自动保存配置**
  - 移除了传统的拖拉滑块，重新设计为直观的三挡开关（`关闭` / `15分钟` / `30分钟`）。
  - 除了保存画布节点外，自动保存现在已覆盖所有生成历史和个人资产库的实时入库。
- **全局 API 配置层统一重构**
  - 彻底清理了原本割裂独立的 Chat / Image / Video API 设置窗口，现已整合为单一的全局设置（`Global API Config`）。支持向前向后无缝的数据迁移。

### 🚀 骨干架构与极限抗压重构 (OOM & Performance Hotfix)

- **彻底根治因为 OOM 导致的隐性闪退（Crash to Desktop）**
  - 我们发现了画布如果承载过多高分辨率图片时，Chromium 原生内存会冲破 2GB 限制而发生闪退崩溃（Render-Process-Gone）。本次升级通过引入 `cache:copy-file` 新型混合底层读写策略，**全面剔除了 ReactFlow OOM 的元凶（Base64 内存黑洞）**！状态管理体积缩水近 1000 倍。
- **修复界面“掉帧卡顿” (Stuttering fixed)**
  - 取消了因用户拖动单节点时，引发大字节 Base64 结构深度复制时的巨慢 GC（垃圾回收）。
  - 实测：即使在同一个画布里摆满 100 张超高清带参考视频的图，也不会再产生过去极其折磨的卡顿或锁死现象。自动保存更加丝滑且全无察觉。

**请注意：如果您在更新后还需要使用过去“全局时期”共享的老资产，系统并没有删除它们，它们仍保留在旧版全局存储空间里。**
