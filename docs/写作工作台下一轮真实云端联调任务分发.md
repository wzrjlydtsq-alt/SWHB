# 写作工作台下一轮真实云端联调任务分发

更新时间：2026-05-07

当前基线：

- ECS/RDS 已部署最新后端。
- `npm run db:migrate` 已在 ECS 上执行。
- `node scripts/smoke-writing.mjs` 已通过，36/36。
- 公网 `http://47.109.138.168/health` 与 `/ready` 正常。
- `APP_ENV=production` 已生效。
- 本地 Mock 链路继续保留；下一轮目标是让桌面端 UI 跑真实 cloud adapter。

---

## 1. Codex 任务 C2：真实 cloud 联调总控与契约守门

负责文件：

- `src/renderer/features/writing/assignmentClient.ts`
- `src/renderer/features/writing/TaskCenterPanel.tsx`
- `src/renderer/services/cloud/writingClient.ts`
- `src/renderer/services/cloud/writingTypes.ts`
- `docs/写作工作台剩余未完成执行计划.md`
- `docs/写作工作台云端闭环测试矩阵.md`

任务：

1. 守住最终后端契约，不再使用 `GET /writing/assignments/me?direction=sent`。
2. 确认 `GET /writing/assignments/me`、`GET /writing/assignments/sent`、`PATCH accept/submit`、通知接口、审核历史接口都能被前端类型覆盖。
3. 协助 GLM-5.1 切换 writing adapter 到 `cloud`，但保留本地 Mock 默认路径。
4. 若 UI 缺少真实 `projectId / episodeId / shotId / assigneeId`，补最小胶水，不伪造云端数字 ID。
5. 最终统一回收所有模型结果，并更新文档状态。

验收：

- `npm run build` 通过。
- `npm test -- --run` 通过。
- `npm --prefix server run build` 通过。
- 桌面端仍能本地 Mock 演示。
- cloud 模式不再因路径、方法、字段错位产生 404/405/400。

---

## 2. GLM-5.1 任务 G5-C：桌面端真实 cloud adapter 联调

负责文件：

- `src/renderer/features/writing/assignmentClient.ts`
- `src/renderer/features/writing/TaskCenterPanel.tsx`
- `src/renderer/features/writing/WritingStudio.tsx`
- `src/renderer/features/writing/useWritingTaskPoller.ts`
- `src/renderer/features/writing/desktopNotify.ts`
- `src/renderer/services/cloud/session.ts`
- 必要时新增少量 cloud 联调开关文件

任务：

1. 增加可控的 writing adapter 切换方式：
   - 默认仍为 `local`。
   - 开发/联调时可切到 `cloud`。
   - 不要把 cloud 写死为默认，避免影响本地演示。
2. 在 cloud 模式下，任务中心使用真实账号数据：
   - 我接收的任务：`GET /writing/assignments/me`
   - 我发出的任务：`GET /writing/assignments/sent`
3. 分发任务时必须使用真实云端数字 ID：
   - `episode_id`
   - `shot_id`
   - `assignee_id`
   - 不允许把本地 Mock 字符串 ID 转成 `0`。
4. 真实任务流转 UI：
   - 导演 A 分发任务。
   - 组员 B 接收任务。
   - 组员 B 提交任务。
   - 导演 A 看到待审核。
5. 轮询提醒：
   - 维持 45 秒轮询。
   - 桌面 Notification 被拒绝时必须降级 toast。
6. 失败态处理：
   - 未登录、无权限、无真实团队/剧集/分镜时给 toast 或明确空状态。
   - 不要静默失败。

验收：

- `npm run build` 通过。
- 本地 Mock 任务中心仍可完整演示。
- cloud 模式下真实接口能获取任务列表。
- 不再出现 `?direction=sent`。
- Notification 授权失败不影响任务中心。

---

## 3. DeepSeek-4-Pro 任务 DS-B：真实联调后端支撑与最小测试数据

负责文件：

- `server/src/routes/writing.ts`
- `server/scripts/smoke-writing.mjs`
- `server/scripts/`
- `docs/写作工作台云端闭环测试矩阵.md`

任务：

1. 保持 ECS/RDS 当前绿色基线：
   - `npm run db:migrate` 不再回退。
   - `node scripts/smoke-writing.mjs` 保持 36/36。
2. 提供或补充一个最小真实联调用测试数据脚本，目标是让前端拿到：
   - 导演账号 A
   - 组员账号 B
   - 同一团队
   - 写作项目
   - 剧集
   - 分镜
   - 可分发给组员 B 的 `assignee_id`
3. 如果前端联调遇到权限问题，优先修权限校验或测试数据，不放宽生产权限。
4. 如需新增脚本，必须不写真实密码，不输出敏感 token。
5. 保持通知、审核历史、评论回复接口可 smoke。

验收：

- ECS 上 `node scripts/smoke-writing.mjs` 仍为 36/36。
- 前端能拿到真实 `projectId / episodeId / shotId / assigneeId`。
- 导演 A 与组员 B 权限符合预期。
- 不引入 `0.0.0.0/0`、硬编码密码或真实 token。

---

## 4. GLM-5V-Tuber 任务 GV-C：真实 cloud 联调视觉验收

负责文件：

- `src/renderer/features/writing/WritingStudio.css`
- `src/renderer/features/writing/TaskCenterPanel.tsx`
- `src/renderer/features/cloud-assets/EpisodeDetailView.tsx`
- `src/renderer/features/cloud-assets/CloudAssetsPanel.css`
- `docs/GLM-5V-Tuber-视觉验收报告.md`

任务：

1. 在真实 cloud 数据状态下验收任务中心：
   - 空状态
   - loading
   - 权限失败
   - 待接收
   - 已接收
   - 已提交
   - 待审核
2. 检查 1366x768 与 1920x1080 两档：
   - 文本不溢出。
   - 按钮不挤压。
   - 状态 badge 不遮挡。
   - toast/通知不会挡住关键操作。
3. 检查单集详情中候选媒体、待审状态、通过/驳回文案是否统一。
4. 如需改 UI，只做小修，不重做布局。

验收：

- `npm run build` 通过。
- 报告写明真实 cloud 数据下的视觉结论。
- 发现的问题按 P0/P1/P2 标注，P0 必须落代码。

---

## 5. Gemini-3.1-Pro 任务 GM-E：真实 cloud 联调矩阵与文档回写

负责文件：

- `docs/写作工作台云端闭环测试矩阵.md`
- `docs/写作工作台剩余未完成执行计划.md`
- `docs/写作工作台多模型回收与修复任务.md`
- `docs/写作软件详细方案与执行计划.md`

任务：

1. 将后端 smoke 状态保持为已通过：
   - ECS/RDS
   - 36/36
   - `APP_ENV=production`
2. 新增“桌面端真实 cloud UI 联调”矩阵：
   - 登录真实账号
   - 选择真实团队
   - 创建/读取写作项目
   - 读取剧集
   - 读取分镜
   - 分发任务
   - 接收任务
   - 提交任务
   - 生成结果回流
   - 导演审核
3. 区分三种状态：
   - 后端 API 已通过
   - 桌面端 UI 已通过
   - 待人工联调
4. 不要再写 RDS 阻塞。

验收：

- 文档没有“RDS 拒绝连接/ECONNREFUSED/等待迁移”的旧口径。
- 每个 UI 联调用例都有明确预置条件、操作步骤、预期结果。
- 若未实际验证，必须标为“待人工联调”，不能写“已通过”。

---

## 6. 下一轮回收顺序

1. DeepSeek 提供真实联调测试数据。
2. GLM-5.1 切 cloud adapter 并跑任务中心真实流转。
3. Codex 修契约/胶水问题并跑 build/test。
4. GLM-5V-Tuber 做真实数据视觉验收。
5. Gemini 更新矩阵与计划。

回收时只认：

- 命令输出。
- UI 截图或明确操作结果。
- 真实接口返回。
- 清楚标注的未完成风险。
