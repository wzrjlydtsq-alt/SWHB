# 写作工作台剩余未完成执行计划

更新时间：2026-05-07

本文只记录当前仍未完成、且需要继续排期的内容。已完成项不再重复展开。当前判断：本地 Mock 演示链路、GLM-5.1 前端契约、GLM-5V-Tuber 视觉整改、Gemini 文档同步均已完成；DeepSeek-4-Pro 的数据库迁移与 HTTP smoke 已在 ECS/RDS 环境跑通，下一步进入前端真实 cloud 联调。

---

## 1. 当前总状态

| 优先级 | 模块 | 状态 | 当前阻塞 | 下一动作 |
| --- | --- | --- | --- | --- |
| P0 | 写作云端数据库迁移 | 已完成 | 无 | 继续保留迁移脚本，后续增量迁移按编号追加 |
| P0 | 写作 API HTTP smoke | 已完成 | 无 | ECS 上 `node scripts/smoke-writing.mjs` 36/36 通过 |
| P0 | 云端闭环测试矩阵最终确认 | 已完成 | 无 | 已按 36/36 结果更新矩阵 |
| P1 | 前端真实 cloud 联调 | 未完成 | 需要真实账号/团队/剧项目数据 | 切换 writing adapter 到 cloud，跑 UI 流程 |
| P1 | WebSocket/轮询通知 | 已完成轮询版，WebSocket 后置 | WebSocket 未做 | 保持 45 秒轮询，真实多人压力后再补 WebSocket |
| P1 | 真实跨端任务提醒 | 基础版已完成 | 需要真实账号联调桌面授权 | toast 已兜底，桌面通知需用户授权 |
| P2 | 导演审核历史和评论线程 | 后端接口已完成，UI 后置 | 不阻塞当前 MVP | DeepSeek-4-Pro 已补接口，Gemini-3.1-Pro 矩阵已列项，GLM-5.1 后续做 UI |

---

## 2. DeepSeek-4-Pro 完成情况：迁移与 smoke

负责文件：

- `server/migrations/004_writing_studio.sql`
- `server/scripts/smoke-writing.mjs`
- `server/src/routes/writing.ts`
- `docs/写作工作台云端闭环测试矩阵.md`

已完成：

- 后端写作路由已注册到 `/api/v1/writing/...`。
- 前端 cloud client 已按最终契约对齐。
- `thumb_oss_key` 字段错误已修正。
- 本地 `npm --prefix server run build` 通过。
- 本地 `npm --prefix server test` 通过。
- ECS 已同步最新后端代码。
- ECS/RDS 已执行 `npm run db:migrate`，`004_writing_studio.sql` 已落库。
- ECS 上 `node scripts/smoke-writing.mjs` 通过，36/36。
- PM2 已重启并 `pm2 save`。
- 公网 `http://47.109.138.168/health` 与 `/ready` 正常，`APP_ENV=production` 已生效。

验收：

- `writing_projects`、`writing_episodes`、`writing_text_assets`、`writing_storyboard_shots`、`writing_shot_media_candidates`、`writing_assignments` 等表存在。
- smoke 覆盖创建项目、创建剧集、文本资产、版本、分镜、候选媒体、任务、接收任务、提交任务、通知、审核历史、评论回复。
- 不再出现 404/405/unknown column。

---

## 3. Gemini-3.1-Pro 完成情况：迁移后更新矩阵

负责文件：

- `docs/写作工作台云端闭环测试矩阵.md`
- `docs/写作工作台多模型回收与修复任务.md`
- `docs/写作工作台剩余未完成执行计划.md`

完成情况：

1. 矩阵第 7 节已从“迁移阻塞”更新为“通过”。
2. DeepSeek P0/P1/P2 编码完成状态已更新为“已验证”。
3. smoke 结果以 ECS 真实输出为准：36 条通过，0 条失败。

验收：

- 文档中“能演示的本地链路”和“真实云端链路”状态不混写。
- 云端闭环状态能追溯到命令输出。

---

## 4. P1：真实通知与跨端提醒

状态：基础轮询版已完成，WebSocket 实时推送后置。

建议负责人：

- DeepSeek-4-Pro：后端通知通道、事件落表、轮询/WebSocket API。
- GLM-5.1：前端任务中心刷新、提醒 UI、小精灵/桌面提示接入。

负责范围：

- 后端通知通道，可能涉及 WebSocket 或短轮询接口。
- 前端任务中心刷新和提醒入口。
- 桌面端小精灵/消息提示。

待定决策：

1. 第一版先做短轮询还是 WebSocket。
2. 通知内容只覆盖任务分发/提交/审核，还是同时覆盖素材发送接收。
3. 离线用户是否需要未读消息表。

建议任务：

1. 已完成：DeepSeek-4-Pro 提供 `GET /writing/notifications`、全部已读、单条已读接口。
2. 已完成：DeepSeek-4-Pro 记录任务分发、提交、审核事件，通知 `type` 使用 `writing.` 前缀。
3. 已完成：GLM-5.1 前端任务中心每 45 秒刷新一次。
4. 已完成：GLM-5.1 接入 toast + 桌面 Notification；如果用户拒绝桌面授权，仍保留 toast 兜底。
5. 已修正：Codex 将 cloud 轮询启用，并将“我发出的任务”查询固定为 `GET /writing/assignments/sent`，不再使用待确认的 `?direction=sent`。
6. 后置：稳定后 DeepSeek-4-Pro 再补 WebSocket 推送，GLM-5.1 切换到实时事件源。

验收：

- 导演分发任务后，组员端不需要手动刷新就能看到新任务。
- 组员提交后，导演端能看到待审核提醒。
- 网络断开时不影响本地写作。

---

## 4.5 P1：前端真实 cloud 联调

状态：未完成，当前最应该做。

负责范围：

- `src/renderer/features/writing/assignmentClient.ts`
- `src/renderer/features/writing/TaskCenterPanel.tsx`
- `src/renderer/services/cloud/writingClient.ts`
- `src/renderer/services/cloud/session.ts`
- 写作工作台 UI 的真实账号/团队/剧项目数据接入

任务：

1. 准备两个真实账号：导演 A、组员 B。
2. 在真实团队下创建写作项目、剧集、分镜。
3. 将 writing adapter 切到 `cloud`，验证任务中心使用真实接口。
4. 导演 A 分发任务，组员 B 接收并提交。
5. 验证任务中心 45 秒轮询能看到新任务/待审状态；桌面通知若未授权，toast 必须出现。
6. 组员生成结果后，验证 `sourceMeta` 能绑定到真实分镜候选媒体接口。
7. 导演审核通过/驳回后，验证素材库和任务中心状态一致。

验收：

- UI 不再只靠 localStorage Mock。
- `GET /writing/assignments/me` 和 `GET /writing/assignments/sent` 都能在真实账号下返回正确数据。
- 失败时给出 toast 或明确错误，不静默失败。
- 不影响本地 Mock 演示模式。

---

## 5. P2：导演审核历史与评论线程

状态：后端接口和 smoke 覆盖已补，前端完整 UI 后置。

建议负责人：

- DeepSeek-4-Pro：审核历史接口、评论线程接口、数据落表。
- Gemini-3.1-Pro：测试矩阵、接口契约、验收文档。
- GLM-5.1：接口稳定后接单集详情 UI 和组员端反馈展示。

负责范围：

- `writing_comments`
- 分镜审核记录
- 单集详情审核区

任务：

1. 已完成：DeepSeek-4-Pro 审核通过/驳回时更新候选媒体状态与反馈。
2. 已完成：DeepSeek-4-Pro 补 `GET /writing/shots/:shotId/review-history`。
3. 已完成：DeepSeek-4-Pro 补 `GET /writing/comments/:commentId/replies`。
4. 已完成：Gemini-3.1-Pro 把审核历史和评论线程加入云端闭环测试矩阵。
5. 已完成：Codex 将通知、审核历史、评论回复加入 `server/scripts/smoke-writing.mjs`。
6. 后置：GLM-5.1 在单集详情中展示最近审核意见，组员端任务卡展示导演反馈。
7. 后续再扩展为完整嵌套回复。

验收：

- 同一分镜多次提交/驳回后能看到历史。
- 导演意见能被组员端看到。
- 不阻塞当前 W1-W8 本地演示。

---

## 6. 当前不用再派的任务

以下内容本轮不用继续派发：

- GLM-5.1：前端 cloud client 与 assignment adapter 契约对齐。
- GLM-5V-Tuber：视觉报告 P0/P1 当前交付口径。
- Gemini-3.1-Pro：本轮文档同步与矩阵创建。
- W1-W8 本地 Mock 链路。

下一轮只围绕前端真实 cloud 联调继续回收；DeepSeek 迁移/smoke、P1/P2 基础接口和轮询版不再重复派发。

具体分发见：`docs/写作工作台下一轮真实云端联调任务分发.md`。

Codex 自领任务见：`docs/写作工作台Codex真实云端联调执行计划.md`。
